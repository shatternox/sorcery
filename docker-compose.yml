version: "3.1"
services:
    www:
        build: .
        ports: 
            - "5000:5000"
        volumes:
            - .:/sorcery
        networks:
            - default
volumes:
    persistent:
